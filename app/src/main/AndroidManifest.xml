<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.easterbrook.flood_it">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup"
        android:fullBackupContent="@xml/backup_descriptor">
        <activity android:name=".activities.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.GameActivity" />
        <activity android:name=".activities.CustomGameSettingsActivity" />
        <activity android:name=".activities.HowToPlayActivity" />
        <activity android:name=".activities.AboutTheAppActivity" />
        <activity android:name=".activities.LeaderboardDisplayActivity"/>
    </application>

</manifest>